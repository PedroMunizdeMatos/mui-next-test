import React from 'react';

const AppBar: React.FC = () => {
  return (
    <div className="hidden lg:flex"> {/* Exibe somente em telas lg */}
      {/* Conteúdo da AppBar */}
    </div>
  );
};

export default AppBar;